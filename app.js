const orb=document.getElementById('orb'),panel=document.getElementById('panel'),state=document.getElementById('state'),chat=document.getElementById('chat'),talk=document.getElementById('talk'),close=document.getElementById('close');const SR=window.SpeechRecognition||window.webkitSpeechRecognition;let rec=null;
function setState(s){orb.className=s;state.textContent={ready:'READY • TAP THE CORE TO SPEAK',listening:'LISTENING…',thinking:'THINKING…',speaking:'SPEAKING…'}[s]||s}
function add(t,c='j'){let d=document.createElement('div');d.className='m '+c;d.textContent=t;chat.appendChild(d);chat.scrollTop=chat.scrollHeight}
function speak(t){if(!speechSynthesis)return;setState('speaking');speechSynthesis.cancel();let u=new SpeechSynthesisUtterance(t);u.onend=()=>setState('ready');speechSynthesis.speak(u)}
function listen(){if(!rec){add('Voice recognition is unavailable here. Try Safari on your iPhone.');return}navigator.mediaDevices?.getUserMedia({audio:true}).then(()=>{try{rec.start()}catch(e){}}).catch(()=>add('Please allow microphone access.'))}
orb.onclick=()=>{panel.classList.add('open');listen()};close.onclick=()=>panel.classList.remove('open');talk.onclick=listen;
if(SR){rec=new SR();rec.lang='en-US';rec.interimResults=false;rec.continuous=false;rec.onstart=()=>setState('listening');rec.onend=()=>setState('ready');rec.onerror=e=>add('Microphone error: '+e.error);rec.onresult=e=>ask(e.results[0][0].transcript.trim())}else setState('ready');
function ask(t){add(t,'u');setState('thinking');let l=t.toLowerCase(),a;if(l.includes('hello')||l.includes('hi jarvis'))a='Hello. JARVIS is online.';else if(l.includes('time'))a='It is '+new Date().toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})+'.';else a='I heard you say: '+t+'. The full AI brain is the next layer.';add(a);speak(a)}